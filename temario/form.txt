 your_name = forms.CharField(label='Your name', max_length=100)

tenemos:
	CharField
	URLField
	ChoiceField

 class Login(forms.Form):

	username = forms.CharField(max_length = 20,
		widget=forms.TextInput(attrs={'id': 'username', 'class': 'validate'}))

	password = forms.CharField( max_length = 20,
		widget=forms.PasswordInput(attrs={'id': 'password', 'class': 'validate'}))			


---------------------------------------------------------------------
class UserCreateForm(UserCreationForm):
    email = forms.EmailField(required=True)

    class Meta:
        model = User
        fields = ("username", "email", "password1", "password2")

    def save(self, commit=True):
        user = super(UserCreateForm, self).save(commit=False)
        user.email = self.cleaned_data["email"]
        if commit:
            user.save()
        return user



        Si es buen lider, sabra como sobrellevar a los programadores, se supone que el buscara a los que saben y los dejara hacer lo que ellos saben hacer


    def clean_valor(self):
